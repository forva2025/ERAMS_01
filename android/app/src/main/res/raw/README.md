Place `ringtone.mp3` here — the Android notification-channel sound for the
`erams_incoming_call` channel (see `lib/services/push_notification_service.dart`),
used when a call comes in while the app is backgrounded/killed.

Separate from the Flutter-asset ringtone at `assets/sounds/ringtone.mp3`
played in-app while the call screen is open in the foreground.
